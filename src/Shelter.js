/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { useRef, useEffect } from 'react'
import { useGLTF, useAnimations } from '@react-three/drei'

export default function Model({ ...props }) {
  const group = useRef()
  const { nodes, materials, animations } = useGLTF('/shelter.glb')
  const { actions } = useAnimations(animations, group)
  useEffect(() => {
    actions.shelter.play()
  })
  return (
    <group ref={group} {...props} dispose={null}>
      <group rotation={[Math.PI / 2, 0, 0.8]} scale={0.018}>
        <primitive object={nodes.mixamorigHips} />
        <skinnedMesh
          geometry={nodes.ely_vanguardsoldier_kerwinatienza_Mesh.geometry}
          material={materials['ely_vanguardsoldier_kerwinatienza_M.002']}
          skeleton={nodes.ely_vanguardsoldier_kerwinatienza_Mesh.skeleton}
        />
      </group>
    </group>
  )
}

useGLTF.preload('/shelter.glb')
